function [  ] = MD_simulation(protein, duration)

global V;

N = length(protein);


%% première itération

for n = 1:N % ensemble des AA
    protein(2*n) = 2*V(10)/V(4)*Force() + 2*V(10)*sqrt(2*V(0.2))*V(7) + protein(2*n-1,1) - V(10)*vo
end

%% boucle principal

for t = 2:duration % variation dans le temps
    
    
    for n = 1:N % ensemble des AA
        
        
    end
end


end

